## 代码格式规范

1. 每行限制在80个字符
2. 大括号单独占一行
3. 使用Tab进行缩进，不使用空格
4. 函数之间使用空行分隔，相关函数可以分组

## 命名规范

5. 采用小驼峰命名变量（camelCase）
6. 函数名采用大驼峰命名（HandleClient），如：`HandleClient()`
7. 宏定义和常量使用全大写字母，单词间用下划线分隔，如：`MAX_REQUEST_SIZE`
8. 结构体类型名使用下划线命名，并以`_t`结尾，如：`RtpHeader_t`
9. 静态函数使用`static`关键字修饰
10. 文件名使用小写字母和下划线

## 变量和内存管理

11. 禁止使用全局变量（特殊情况可使用`static`修饰的模块级变量）
12. 禁止使用extern
13. 保证申请的内存都要释放，使用`malloc`/`calloc`后必须对应`free`
14. 指针使用前必须检查是否为NULL
15. 释放内存后立即将指针设置为NULL，避免悬空指针

## 函数规范

16. 函数参数不超过5个，超过时应考虑使用结构体封装
17. 函数功能单一，一个函数只做一件事
18. 函数长度不超过100行，超过时应考虑拆分
19. 函数必须有明确的返回值，错误时返回负数或NULL
20. 函数参数中，输入参数在前，输出参数在后

## 注释规范

21. 每个源文件开头必须有文件头注释，说明文件功能
22. 每个函数必须有函数注释，使用Doxygen风格，包含：功能描述、参数说明、返回值说明
23. 复杂逻辑必须添加行内注释说明
24. 注释与代码同步更新，过时的注释要及时删除

## 错误处理

25. 所有可能失败的系统调用必须检查返回值
26. 错误信息要明确，便于调试和定位问题
27. 使用统一的错误码定义，避免使用魔法数字

## 代码组织

28. 头文件包含顺序：系统头文件、第三方库头文件、项目头文件
29. 使用`#ifndef`/`#define`/`#endif`保护头文件，避免重复包含
30. 相关功能的代码组织在一起，使用注释分隔不同功能模块
31. 常量定义集中在文件开头，使用`#define`或`const`声明

## 其他规范

32. 循环和条件语句即使只有一行代码，也要使用大括号
33. switch语句必须包含default分支
34. 比较常量时，常量放在左边，如：`if (NULL == ptr)`
35. 每个模块都要有内部头文件和外部头文件
